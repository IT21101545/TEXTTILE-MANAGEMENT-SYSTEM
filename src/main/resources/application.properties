# --- Active profile ---
# dev  = H2 in-memory (no SQL Server needed) — good for UI work
# sqlserver = real database on localhost\SQLEXPRESS (run db/setup-textile-database.sql first)
spring.profiles.active=dev

server.port=8080
